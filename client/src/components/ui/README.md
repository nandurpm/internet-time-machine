# Ui

## Purpose

Contains reusable, mostly Radix-based UI primitives shared across dashboard pages.

## Contents

- `accordion.tsx` — Provides the reusable accordion UI primitive and styling contract used by the React client.
- `alert-dialog.tsx` — Provides the reusable alert dialog UI primitive and styling contract used by the React client.
- `alert.tsx` — Provides the reusable alert UI primitive and styling contract used by the React client.
- `aspect-ratio.tsx` — Provides the reusable aspect ratio UI primitive and styling contract used by the React client.
- `avatar.tsx` — Provides the reusable avatar UI primitive and styling contract used by the React client.
- `badge.tsx` — Provides the reusable badge UI primitive and styling contract used by the React client.
- `breadcrumb.tsx` — Provides the reusable breadcrumb UI primitive and styling contract used by the React client.
- `button-group.tsx` — Provides the reusable button group UI primitive and styling contract used by the React client.
- `button.tsx` — Provides the reusable button UI primitive and styling contract used by the React client.
- `calendar.tsx` — Provides the reusable calendar UI primitive and styling contract used by the React client.
- `card.tsx` — Provides the reusable card UI primitive and styling contract used by the React client.
- `carousel.tsx` — Provides the reusable carousel UI primitive and styling contract used by the React client.
- `chart.tsx` — Provides the reusable chart UI primitive and styling contract used by the React client.
- `checkbox.tsx` — Provides the reusable checkbox UI primitive and styling contract used by the React client.
- `collapsible.tsx` — Provides the reusable collapsible UI primitive and styling contract used by the React client.
- `command.tsx` — Provides the reusable command UI primitive and styling contract used by the React client.
- `context-menu.tsx` — Provides the reusable context menu UI primitive and styling contract used by the React client.
- `dialog.tsx` — Provides the reusable dialog UI primitive and styling contract used by the React client.
- `drawer.tsx` — Provides the reusable drawer UI primitive and styling contract used by the React client.
- `dropdown-menu.tsx` — Provides the reusable dropdown menu UI primitive and styling contract used by the React client.
- `empty.tsx` — Provides the reusable empty UI primitive and styling contract used by the React client.
- `field.tsx` — Provides the reusable field UI primitive and styling contract used by the React client.
- `form.tsx` — Provides the reusable form UI primitive and styling contract used by the React client.
- `hover-card.tsx` — Provides the reusable hover card UI primitive and styling contract used by the React client.
- `input-group.tsx` — Provides the reusable input group UI primitive and styling contract used by the React client.
- `input-otp.tsx` — Provides the reusable input otp UI primitive and styling contract used by the React client.
- `input.tsx` — Provides the reusable input UI primitive and styling contract used by the React client.
- `item.tsx` — Provides the reusable item UI primitive and styling contract used by the React client.
- `kbd.tsx` — Provides the reusable kbd UI primitive and styling contract used by the React client.
- `label.tsx` — Provides the reusable label UI primitive and styling contract used by the React client.
- `menubar.tsx` — Provides the reusable menubar UI primitive and styling contract used by the React client.
- `navigation-menu.tsx` — Provides the reusable navigation menu UI primitive and styling contract used by the React client.
- `pagination.tsx` — Provides the reusable pagination UI primitive and styling contract used by the React client.
- `popover.tsx` — Provides the reusable popover UI primitive and styling contract used by the React client.
- `progress.tsx` — Provides the reusable progress UI primitive and styling contract used by the React client.
- `radio-group.tsx` — Provides the reusable radio group UI primitive and styling contract used by the React client.
- `resizable.tsx` — Provides the reusable resizable UI primitive and styling contract used by the React client.
- `scroll-area.tsx` — Provides the reusable scroll area UI primitive and styling contract used by the React client.
- `select.tsx` — Provides the reusable select UI primitive and styling contract used by the React client.
- `separator.tsx` — Provides the reusable separator UI primitive and styling contract used by the React client.
- `sheet.tsx` — Provides the reusable sheet UI primitive and styling contract used by the React client.
- `sidebar.tsx` — Provides the reusable sidebar UI primitive and styling contract used by the React client.
- `skeleton.tsx` — Provides the reusable skeleton UI primitive and styling contract used by the React client.
- `slider.tsx` — Provides the reusable slider UI primitive and styling contract used by the React client.
- `sonner.tsx` — Provides the reusable sonner UI primitive and styling contract used by the React client.
- `spinner.tsx` — Provides the reusable spinner UI primitive and styling contract used by the React client.
- `switch.tsx` — Provides the reusable switch UI primitive and styling contract used by the React client.
- `table.tsx` — Provides the reusable table UI primitive and styling contract used by the React client.
- `tabs.tsx` — Provides the reusable tabs UI primitive and styling contract used by the React client.
- `textarea.tsx` — Provides the reusable textarea UI primitive and styling contract used by the React client.
- `toggle-group.tsx` — Provides the reusable toggle group UI primitive and styling contract used by the React client.
- `toggle.tsx` — Provides the reusable toggle UI primitive and styling contract used by the React client.
- `tooltip.tsx` — Provides the reusable tooltip UI primitive and styling contract used by the React client.

## Responsibilities

Keep generic presentation primitives here; monitoring behavior and route-level state belong in application components or pages.

## Important Notes

- This folder is part of **Internet Time Machine** and should be kept consistent with the commands and architecture documented in the root README.
- Paths and file roles listed above reflect the current repository implementation.

